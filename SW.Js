// Service worker minimal : sa seule présence permet au navigateur de proposer // "Installer l'application". Pas de cache complexe pour l'instant — l'appli // a besoin d'une connexion à Supabase de toute façon pour fonctionner. self.addEventListener('install', () => self.skipWaiting()); self.addEventListener('activate', () => self.clients.claim()); self.addEventListener('fetch', () => {}); // requis par certains navigateurs pour considérer l'appli "installable"
